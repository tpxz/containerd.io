/.idea/

# macOS leftovers
.DS_Store

# Node.js-related assets (e.g. for CSS)
node_modules/

# Hugo-generated assets
public/
resources/
.hugo_build.lock

# Link checker assets
bin/
tmp/

# Documentation imported from the containerd submodules.
# NOTE: unlike upstream, this fork commits content/docs/ and static/docs/ --
# the Chinese translation lives in content/docs/ and has to survive a fresh
# clone. Re-import with `make materialize-docs` only when bumping upstream.
